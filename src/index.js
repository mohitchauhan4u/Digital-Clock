/* It's importing the React library, the ReactDOM library, and the App component. */
import React from "react";
import ReactDOM from "react-dom";
import App from './App'

ReactDOM.render(<App/>, document.getElementById("root"));
